# Claude Code local runtime state + marketplace skill installs (re-installable, not dotfiles)
.claude/
.agents/
skills-lock.json

# Quickshell manages this next to shell.qml; machine-specific (see CLAUDE.md)
.qmlls.ini

# CMake build output
shell/build/

# Reference clones / scratch (never committed)
_upstream/
